<h1>Food Item</h1>

<p>
  <b>Title</b><br>
  <%= @food_item.title %>
</p>

<p>
  <b>Price</b><br>
  <%= @food_item.description %>
</p>

<div class="form-actions">
  <%= link_to 'Back', food_items_path, :class => 'btn'  %>
  <%= link_to 'Edit', edit_food_item_path(@foot_item), :class => 'btn' %>
  <%= link_to 'Delete', food_item_path(@foot_item), :method => 'delete', :confirm => 'Are you sure?', :class => 'btn btn-danger' %>
</div>